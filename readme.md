# Laraue.Apps.MarkdownTranspiler

The application provides Web API to transform passed Markdown file to HTML.  
Built on [Laraue.Interpreter.Markdown](https://github.com/win7user10/Laraue.Interpreter) library.

### Endpoints
`POST: api/markdown/transpile` - process markdown and return object with the HTML contents